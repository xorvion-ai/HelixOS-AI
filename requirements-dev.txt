# Dev/test dependencies (not needed to run the app).
#   pip install -r requirements-dev.txt --trusted-host pypi.org --trusted-host files.pythonhosted.org
-r requirements.txt
uvicorn[standard]==0.34.0   # local dev server only (Vercel invokes the ASGI app directly)
pytest>=8.0
httpx>=0.27          # FastAPI TestClient transport

# Optional, for the durable Postgres LangGraph checkpointer (set DATABASE_URL):
#   langgraph-checkpoint-postgres
#   psycopg[binary]
